{% extends 'layout.html.twig' %}
{% block title %}Formules{% endblock %}
{% block content %}

<section class="pricing py-5">
  <div class="container">
    <div class="row">
{% for formula in formulas %}
<div class="col-lg-4">
        <div class="card mb-5 mb-lg-0">
          <div class="card-body">
		  	<h2 class="card-price text-center">{{formula.name}}</h2>
			<h6 class="card-price text-center">{{formula.price}}<span class="period"> €</span></h6>
            <h5 class="card-title text-muted text-uppercase text-center">Promotion</h5>
            <hr>
            <ul class="fa-ul">
              <li><span class="fa-li"></span>{{formula.description}}</li>
            </ul>
            <a href="#" class="btn btn-block btn-primary text-uppercase">Button</a>
          </div>
        </div>
      </div>


{% endfor %}
{% endblock %}